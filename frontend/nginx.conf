server {
  listen 80;
  server_name _;

  gzip on;
  gzip_types text/plain text/css application/javascript application/json image/svg+xml;

  root /usr/share/nginx/html;

  # Single-page app fallback (if you later route on client)
  location / {
    try_files $uri /index.html;
  }
}
